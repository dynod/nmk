refs:
    # Base plugin
    - plugins/base/plugin.yml

config:
    # Nmk dependencies for venv
    venvFileDeps:
        - ${PROJECTDIR}/nmk-requirements.txt

    # For now, use make on Linux for CI
    loadMeSysDeps:
        make:
            apt: make

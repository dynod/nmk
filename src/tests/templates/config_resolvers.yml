config:
    someValue: dynamic
    someResolved:
        __resolver__: tests.sample_resolvers.StrResolver
        params:
            qualifier: ${someValue}

    someBool:
        __resolver__: tests.sample_resolvers.BoolResolver

    someOverridableBool:
        __resolver__: tests.sample_resolvers.BoolResolver
